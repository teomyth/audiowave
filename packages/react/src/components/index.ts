/**
 * Components - Unified exports
 *
 * Exports all React components for audio visualization.
 */

// Re-export types for convenience
export type { AudioWaveProps, RenderContext } from '../types';
export { AudioWave } from './AudioWave';
